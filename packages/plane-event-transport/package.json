{
	"name": "cyrus-plane-event-transport",
	"version": "0.0.1",
	"description": "Plane (Community Edition) event transport for receiving and verifying Plane webhooks",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"dev:server": "tsx scripts/dev-runner.ts",
		"smoke:tracker": "tsx scripts/smoke-tracker.ts",
		"typecheck": "tsc --noEmit",
		"test": "vitest",
		"test:run": "vitest run --passWithNoTests"
	},
	"dependencies": {
		"cyrus-core": "workspace:*",
		"fastify": "^5.8.5"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"tsx": "^4.20.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
